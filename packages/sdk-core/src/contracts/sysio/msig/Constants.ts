/** Default account that hosts the Wire system multisig contract. */
export const DEFAULT_MSIG_CONTRACT = "sysio.msig"

/** Contract chunk boundary used by current chunked `sysio.msig` deployments. */
export const PROPOSAL_CHUNK_SIZE = 200 * 1024
