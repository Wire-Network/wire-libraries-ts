/** Default account hosting the Wire token registry. */
export const DEFAULT_TOKENS_CONTRACT = "sysio.tokens"

/** Default maximum rows read from either registry table. */
export const DEFAULT_TOKEN_QUERY_LIMIT = 500
